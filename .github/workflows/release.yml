name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    name: Create Release
    runs-on: ubicloud-standard-2

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Get version
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Cross-compile binaries
        run: make cross-compile

      - name: Create checksums
        run: |
          cd bin
          sha256sum claude-matrix-* > checksums.txt
          cat checksums.txt

      - name: Create release notes
        id: release_notes
        run: |
          cat > release_notes.md << 'EOF'
          ## Installation

          Download the appropriate binary for your platform:

          ### macOS
          ```bash
          # Intel Mac
          curl -LO https://github.com/mateimicu/tmux-claude-matrix/releases/download/${{ steps.version.outputs.version }}/claude-matrix-darwin-amd64
          chmod +x claude-matrix-darwin-amd64
          sudo mv claude-matrix-darwin-amd64 /usr/local/bin/claude-matrix

          # Apple Silicon Mac
          curl -LO https://github.com/mateimicu/tmux-claude-matrix/releases/download/${{ steps.version.outputs.version }}/claude-matrix-darwin-arm64
          chmod +x claude-matrix-darwin-arm64
          sudo mv claude-matrix-darwin-arm64 /usr/local/bin/claude-matrix
          ```

          ### Linux
          ```bash
          # x86_64
          curl -LO https://github.com/mateimicu/tmux-claude-matrix/releases/download/${{ steps.version.outputs.version }}/claude-matrix-linux-amd64
          chmod +x claude-matrix-linux-amd64
          sudo mv claude-matrix-linux-amd64 /usr/local/bin/claude-matrix

          # ARM64
          curl -LO https://github.com/mateimicu/tmux-claude-matrix/releases/download/${{ steps.version.outputs.version }}/claude-matrix-linux-arm64
          chmod +x claude-matrix-linux-arm64
          sudo mv claude-matrix-linux-arm64 /usr/local/bin/claude-matrix
          ```

          ### Verify Installation
          ```bash
          claude-matrix --help
          ```

          ## What's New

          See [CHANGELOG.md](https://github.com/mateimicu/tmux-claude-matrix/blob/main/CHANGELOG.md) for detailed changes.

          ## Checksums

          \`\`\`
          $(cat bin/checksums.txt)
          \`\`\`
          EOF

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            bin/claude-matrix-darwin-amd64
            bin/claude-matrix-darwin-arm64
            bin/claude-matrix-linux-amd64
            bin/claude-matrix-linux-arm64
            bin/checksums.txt
          body_path: release_notes.md
          draft: false
          prerelease: false
          generate_release_notes: true
